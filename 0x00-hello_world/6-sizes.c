#include <stdio.h>

/**
 * main - Entry point
 *
 * Return:Always 0 (Success) 
 */
int main(void)
{
  char acharacter;
  inte aninteger;
  long along;
  long long longalong;
  float afloat;
  
   printf("size of  char : %lu byte(s)/n", size  of (a character));
   printf("size of an int : %lu byte(s)/n", size  of (aninteger));
   printf("size of  along : %lu byte(s)/n", size  of (a long));
   printf("size of  a long long int: %lu byte(s)/n", size  of (a longalong));
   printf("size of  a float: %lu byte(s)/n", size  of (a float));
    return 0;
}
