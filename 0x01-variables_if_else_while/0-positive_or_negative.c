#include <studio.h>

/**
 * main -entry point
 *
 * Return :always 0
 */
int main(void)
{
	if ( n > 0 )
	printf("%d is positive/n" , n);
	else if ( n==0)
	printf("%d is zero/n" , n);
	printf("%d is  negative\n", n);
	return (0);
}
