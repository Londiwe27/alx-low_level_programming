#include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints a random number and states whether
 ** it is positive, negative, or zero.
 * Return: Always 0.
 */
into main(void)
{
	else if (n < 0)
		printf("%d is negative\n", n);
	else
		printf("%d is zero\n", n);

	return (0);
}
